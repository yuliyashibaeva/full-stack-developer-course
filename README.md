# full-stack-developer-course

Learning the course https://fullstackopen.com/en/

# Exercises 3.9.-3.11.

From the part https://fullstackopen.com/en/part3/deploying_app_to_internet

## Application

The application is deployed here https://full-stack-developer-course-julia.onrender.com/

### Methods

1. **GET** `/api/persons`
2. **GET** `/api/info`
3. **GET** `/api/persons/{id}`
4. **DELETE** `/api/persons/{id}`
5. **POST** `/api/persons`
6. **PUT** `/api/persons/{id}`

   **Request body:**

   ```json
   {
     "name": "name",
     "number": "123 456 78 90"
   }
   ```
